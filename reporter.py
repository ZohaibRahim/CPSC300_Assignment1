
def log_start(patient, t, rooms_available)

def log_treatment_completed(patient, t)

def log_admission_complete(patient, t) # print admission at completion time

def log_departure(patient, t, rooms_available)

def final_report(patients) # per-patient total wait + totals/average

def note_wait_segment(patient, key, delta) # accumulate waits: assess, to_treat, admit
